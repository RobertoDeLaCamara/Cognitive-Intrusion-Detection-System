# Packet capture
scapy>=2.6.2

# ML
scikit-learn>=1.3.0
numpy>=1.24.0
# torch is installed in Dockerfile to force CPU version

# Model persistence
joblib>=1.3.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
websockets>=12.0
python-multipart>=0.0.22
starlette>=0.49.1

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Optional: PostgreSQL
# asyncpg>=0.29.0

# Auth (Phase 7)
PyJWT>=2.8.0
bcrypt>=4.1.0

# Optional: MLflow model registry (Phase 5)
# mlflow>=2.22.4

# Optional: Prometheus metrics (Phase 7)
# prometheus-client>=0.20.0

# Optional: OpenTelemetry tracing (Phase 7)
# opentelemetry-api>=1.22.0
# opentelemetry-sdk>=1.22.0
# opentelemetry-exporter-otlp>=1.22.0
# opentelemetry-instrumentation-fastapi>=0.43b0

# Optional: Streamlit dashboard (Phase 6)
# streamlit>=1.30.0

# Optional: GeoIP enrichment (Phase 8)
# geoip2>=4.8.0

# Optional: Webhook notifications (Phase 8)
httpx>=0.26.0

# Transitive dependency pins (security)
urllib3>=2.6.3
wheel>=0.46.2
sqlparse>=0.5.4
pyasn1>=0.6.2
werkzeug>=3.1.5
fonttools>=4.60.2
aiohttp>=3.13.3
pillow>=12.1.1

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Pin anyio to 3.x for pytest-asyncio compatibility
# (pytest-asyncio <0.24 requires anyio 3.x; remove this pin when upgrading)
anyio>=3.7.0,<4.0.0
